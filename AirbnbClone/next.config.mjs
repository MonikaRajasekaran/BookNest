/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com','cdn.pixabay.com','a0.muscache.com',],
  },
};

export default nextConfig;
